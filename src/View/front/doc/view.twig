<article id="view-home"
    class="menu-target3">
    <h2>
        <i class="fas fa-desktop"></i> 
        View
    </h2>

    <nav class="sidebar">
        <input id="sidebar-view"
            class="sidebar-check" 
            type="checkbox">
        <label for="sidebar-view"
            class="sidebar-open" 
            title="Show Sidebar">
            <i class="far fa-eye fa-lg fa-fw"></i>
        </label>
        <label for="sidebar-view"
            title="Hide Sidebar">
            <i class="far fa-eye-slash fa-lg fa-fw"></i>
        </label>

        {% for class in classes %}
            {% if 'View' in class.path %}
            <a href="#{{ class.class|lower }}"
                title="{{ class.class }}">
                {{ loop.index }}
            </a>
            {% endif %}
        {% endfor %}

        <a href="#view-home" 
            title="view Home">
            <i class="fas fa-chevron-circle-up"></i>
        </a>
    </nav>

    <ul class="flex">

        {% for class in classes %}
            {% if 'View' in class.path %}
                <li>
                    <a  class="button-secondary" 
                        href="#{{ class.class|lower }}">
                        {{ loop.index }} - 
                        {{ class.class }}
                    </a>
                </li>
            {% endif %}
        {% endfor %}

    </ul>

    {% for class in classes %}
        {% if 'View' in class.path %}
            {{ include('front/doc/docMVC.twig') }}
        {% endif %}
    {% endfor %}

</article>
