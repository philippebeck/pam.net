<article id="model-home"
    class="menu-target2">
    <h2>
        <i class="fas fa-database"></i> 
        Model
    </h2>

    <nav class="sidebar">
        <input id="sidebar-model"
            class="sidebar-check" 
            type="checkbox">
        <label for="sidebar-model"
            class="sidebar-open" 
            title="Show Sidebar">
            <i class="far fa-eye fa-lg fa-fw"></i>
        </label>
        <label for="sidebar-model"
            title="Hide Sidebar">
            <i class="far fa-eye-slash fa-lg fa-fw"></i>
        </label>

        {% for class in classes %}
            {% if 'Model' in class.path %}
            <a href="#{{ class.class|lower }}"
                title="{{ class.class }}">
                {{ loop.index }}
            </a>
            {% endif %}
        {% endfor %}

        <a href="#model-home" 
            title="Model Home">
            <i class="fas fa-chevron-circle-up fa-lg fa-fw"></i>
        </a>
    </nav>

    <ul class="flex">

        {% for class in classes %}
            {% if 'Model' in class.path %}
                <li>
                    <a class="button-secondary" 
                        href="#{{ class.class|lower }}">
                        {{ loop.index }} - 
                        {{ class.class }}
                    </a>
                </li>
            {% endif %}
        {% endfor %}

    </ul>

    {% for class in classes %}
        {% if 'Model' in class.path %}
            {{ include('front/docMVC.twig') }}
        {% endif %}
    {% endfor %}

</article>
